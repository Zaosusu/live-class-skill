---
name: live-class-skill
description: "社媒直播课 / 线上课程「代听」助手（macOS 版，Windows 版见 win/SKILL_win.md）：用户没时间跟社媒平台上的直播课/线上课程（B站/抖音/小红书/视频号/快手/千聊/小鹅通等），指导其自行打开直播网址出声后，持续录制系统声音（mac 用系统自带 ScreenCaptureKit，零安装）→ 本地离线增量转写（近流式实时出字）→ 结束后生成结构化总结笔记。推荐一条命令 `listen.py --url <链接>` 听完整场：B站自动抓主播/主题并等下播自动收尾；抖音/小红书等盲录平台连续静音自动收尾。⚠️ 绝不使用任何浏览器自动化（防封号）。当用户说「帮我听/代听这场直播课」「把 B站/抖音/小红书直播课录下来转成文字」「没时间上课/看直播，结束后给我一份笔记/总结」，或给出一个直播/课程 URL 要求收音转写并总结时使用。会议类（腾讯会议/飞书会议/Zoom）已自带 AI 纪要总结，非本技能目标场景。"
agent_created: true
---

# live-class-skill（macOS 版）— 社媒直播课/线上课程「代听」助手

用户没时间一直盯直播，由本技能完成：**用户打开直播网页出声 → 持续收音 → 本地转写（实时出字）→ 结束后总结**。
产出两份文件：① 逐字稿 `transcript.txt`；② 结构化总结 `summary.md`。

> **定位边界（先读）**：本技能面向**没有回放、也没有自带总结**的社媒直播课与线上课程
> （B站/抖音/小红书/视频号/快手直播课、千聊/小鹅通/知识星球等付费课）。
> 会议类（腾讯会议/飞书会议/Zoom 等）**已内置 AI 纪要总结**，直接用其自带纪要即可，不需要本技能代听；
> 仅当某场会议确无 AI 纪要又必须代听时，本技能技术上同样能录（见
> [`references/platforms.md`](references/platforms.md)），但非主场景。

> **运行平台选择**：
> - **macOS** → 本文即完整流程（ScreenCaptureKit 收音，零安装）。
> - **Windows** → 改用 **[`win/SKILL_win.md`](win/SKILL_win.md)**（WASAPI loopback 内录）。
> 两版共用同一离线转写引擎与模型（仓库根 `scripts/`）。
>
> ⚠️ **反浏览器自动化铁律**：本技能只监听系统声音，**绝不驱动/注入/读取浏览器**。
> 打开直播网页由用户自己完成。所有 agent 须遵守仓库根 [`AGENTS.md`](AGENTS.md)。

## ⚠️ 铁律：零浏览器自动化（防封号）

本技能**只监听系统正在播放的声音**，**绝不驱动、注入、控制、读取任何浏览器/App**。
打开直播网页这一步由**用户自己**在浏览器里完成，或在桌面 App（如小鹅通/千聊客户端）里打开。

- ✅ 允许：提示/指导用户打开某个 URL、确认页面在播放且音量非 0、监听系统声音、离线转写、总结。
- ❌ 禁止：selenium / playwright / puppeteer / webdriver / CDP / 浏览器插件注入 / 模拟点击输入 / 读取页面 DOM / 抓直播流地址。
  一句话：**所有 agent 不得调用任何"浏览器自动化"能力来配合本技能**（详见仓库根 `AGENTS.md`）。

## 核心原理

```
浏览器/App 播放直播声音 ──▶ macOS 系统混音器
                                   │ ScreenCaptureKit（macOS 14+ 系统自带，无需任何安装）
                                   ▼
              capture 工具（scripts/bin/capture）近流式分块录音(16kHz 单声道)
                                   │
              transcribe.py 增量转写（sherpa-onnx 本地离线，GPU优先/CPU兜底）
                                   ▼
        transcripts/transcript.txt（边听边追加）→ 主代理生成总结 summary.md
```

关键事实（决定整个流程怎么做）：
- **收音零安装**：直接用 macOS 系统自带的 **ScreenCaptureKit** 捕获"正在播放的系统声音"
  （OBS 等录屏工具同款官方接口），**不需要虚拟声卡（BlackHole）、不需要 ffmpeg、
  不需要切换系统输出设备、不干扰正常听声**。唯一前置：一次性授予
  **「屏幕录制」隐私权限**（系统设置→隐私与安全性→屏幕录制，勾选运行本工具的 App）。
- **静音不落盘**：系统没有声音在播放时不产生音频分块（与 win 版一致），因此可用
  「连续静音时长」作为直播结束的判据之一。
- **转写引擎 sherpa-onnx 本地离线**（中文 zipformer-ctc int8 模型，约 370MB，
  缓存于 `~/.cache/live-class-skill/models/`），不联网、不花钱、速度快（远超实时）。
  执行档位自动 **GPU优先 / CPU兜底**（mac 无 NVIDIA GPU → 自动 CPU，开箱即用）。
- **转录是近流式增量的**：录音按 5s 左右高频落小分块、转写秒级增量出字，随时可看已转内容，中断可续。

## 推荐主流程：一条命令听完整场（listen.py）

一键自动完成：认链接 → (B站等能抓则)抓主播/主题建目录 → 内录(近流式小分块) →
秒级增量转写出字 → 结束检测(下播/静音超时/定时) → 自动停录收尾 → 产出最终 `transcript.txt`。
可 `nohup` 无人值守，也可前台挂着看实时出字。

### 第 1 步：收集信息
确认以下信息（用户已给齐则跳过）：
- 直播/课程 **URL**（任意平台，或 B站房间号）；
- **结束信号**：全平台统一支持**连续静音 `--silent-timeout` 分钟（默认 3）自动收尾**；
  B站另有 live_status 下播接口更快触发；抖音/小红书等盲录平台无接口则完全靠静音超时；
  默认都可用用户说「结束/好了」+ Ctrl-C 立即收尾；
- **听课人标识** `--user`（可选，只记入 meta.json）。

### 第 2 步：环境检查（每次开始前必跑）
```bash
bash <技能目录>/scripts/setup.sh
```
- 全部 `[OK]` → 继续。有 `[缺失]` → 按指引修复；也可直接 `bash setup.sh --fix`
  （自动建 `scripts/.venv` 装 sherpa-onnx、下载模型、编译 capture，需联网几分钟）。
- setup 会**自动探测加速档位**并打印判定（mac 通常为 CPU 兜底，无需任何操作；
  有 NVIDIA eGPU/外置加速时才可能走 GPU）。
- **「屏幕录制」权限未授予**（首次使用必经）：
  1. 打开 系统设置 → 隐私与安全性 → **屏幕录制**；
  2. 勾选运行本工具的 App（终端 / IDE，即从哪启动本技能就勾哪个）；
  3. **完全退出并重启该 App**（权限变更需重启生效）后，让用户回复「好了」，
     再重跑 setup.sh 确认权限 OK。
- 也可直跑 `python3 <技能目录>/scripts/common.py` 看探测结果。

### 第 3 步：启动代听（单条命令）
```bash
cd "<技能目录>"
PY="<setup.sh 显示出的 python 路径，默认 scripts/.venv/bin/python>"

# 给任意直播链接即可：B站自动抓主播/主题；抖音/小红书等自动降级纯时间目录照常内录
"$PY" scripts/listen.py --url <任意直播URL> --user <你的标识>

# B站数字简写：--room <房间号>
# 没开播想等：加 --wait-start [--max-wait-min 60]
# 无人值守整场：前面加 nohup … > user/listen_me.log 2>&1 &（B站自动等下播/下播收尾）
```
- 脚本会打印**会话目录**（落盘位置见第 4 步规范）、分块秒数、收尾方式，然后启动
  内录 + 转写两个子进程并进入监控循环。
- 盲录平台（抖音/小红书/视频号等无公开接口）额外提示：请自行打开直播页出声；
  默认**连续静音 3 分钟**自动收尾（`--silent-timeout` 可调），可叠加
  `--max-minutes` 定时兜底，或 Ctrl-C 手动收尾。
- **静音超时对 B站同样生效**（全平台统一）：B站下播有 live_status 接口、更快更准；
  但直播流在播却长时间无声（主播挂机/画面停滞）时，由同一静音超时兜底收尾。
- 想先看目录不实际录音：`--plan-only`。

### 第 4 步：用户打开直播并确认「有声音进录音」（关键！）
1. **指导用户自己在浏览器手动打开直播 URL**（本技能不驱动浏览器，防封号，见铁律）；
   需登录则引导用户扫码/登录（登录态浏览器侧保留）；
2. **打开后，主动向用户发一次固定提醒**（浏览器首次打开常拦截自动播放/默认静音，是无声最常见原因）：
   > 请确认直播**在播放且能听到声音**。没声音就依次检查：① 地址栏喇叭图标未静音；
   > ② 页面/播放器音量非 0；③ 系统音量非 0；④ 页面确实在播放。
   > 只要你能听到声即可，本技能只录「系统正在播放的声音」。
3. 等第一个分块落地并转出文字（约 5–10 秒）：
   ```bash
   tail -1 "<会话目录>/transcripts/segments.jsonl"
   ```
   - 有文字 → 链路通了，向用户汇报「已开始代听」，进入监控。
   - 为空/(静音) → 再提示用户按上面 ①–④ 排查（常见是标签页静音或没点播放）；仍无则查
     ⑤ 平台是否要登录/权限；修复后重试。

### 第 5 步：监听进展（低频轮询）
每 1–2 分钟对比一次 `<会话目录>/transcripts/segments.jsonl` 行数（或 transcript.txt 字数）增量：
- 持续增长 → 正常，不打扰用户；
- **停止增长 ≥ 3 分钟** → 询问用户「收音停了，是课间/结束/卡住了吗？」再处理；
- 用户中途嘱托「帮我记一下 XX」→ 记下，收尾总结时单独成节回应，不打断录音。

### 第 6 步：收尾（自动或手动）
- **B站**：脚本轮询 live_status，连续 N 次（默认 5，约 2.5 分钟）检测到下播 → 自动收尾；
  直播流在播但长时间无声（主播挂机/画面停滞）→ 同一静音超时兜底收尾；
- **盲录平台**：连续 `--silent-timeout` 分钟（默认 3）无真实声音（读音频能量判定，
  静音期间的分块不刷新基准）→ 自动收尾；
- 三者之外：到 `--max-minutes` 定时兜底、或用户说「结束/好了」时 Ctrl-C / 让前台进程收尾。
- 收尾自动执行：停止内录(优雅落盘尾块) → 通知转写处理完残余 → 产出最终 `transcript.txt`。
  脚本退出时会打印「本次监听 X 分钟，文字稿 Y 字」与最终稿路径。

### 第 7 步：生成总结（核心增值步骤）
1. 读 `transcript.txt` 全文（长文可分段读）；
2. 明显重复段落（回声/重播导致）可在总结里忽略，但**不改写逐字稿原文件**；
3. 生成 `summary.md` 到会话目录，按课程/直播性质取舍结构：
```markdown
# <课程/直播课名> — 直播代听笔记（<日期>）

## 一句话摘要
## 内容脉络（分节标题，标注大致时间）
## 关键要点（编号列出，可附原文短句）
## 行动项（要做的事 / 待准备 / 下节预告）
## 数据与金句（如有）
## 存疑/没听懂（便于用户回看对应时段）
```
4. 用户中途嘱托的「帮我记一下 XX」在此单独成节回应。

### 第 8 步：交付
- 用结果展示把 `summary.md` 与 `transcript.txt` 呈现给用户；
- 口头汇报 3–5 句：讲了什么、几个关键点、文件存放位置；
- 询问是否需要导出飞书文档/其他格式（用户要才做，不主动）。

## 会话落盘规范（单一可信源）

`listen.py` 一律把产出落 `<技能目录>/user/session/<名字>/`，一场一个目录，命名走降级链：

```
user/session/<主播账号>_<直播主题>_<YYYYMMDD-HHMM>/   ← B站等能抓公开元数据时
user/session/<YYYYMMDD-HHMM>/                        ← 抓不到(抖音/小红书/视频号等)降级纯时间
    ├── meta.json                 # source/url/主播/主题/听课人/开始时间/段长/engine
    ├── chunks/seg_*.wav          # 音频分块（listen 默认 5s 一段，近流式）
    ├── transcripts/
    │   ├── segments.jsonl       # 逐段结构化转写（实时追加）
    │   └── transcript.txt       # 纯文字稿（实时追加，最终交付物）
    ├── record.log / transcribe.log / listen.log
    └── summary.md                # 结束时主代理生成的总结（放同一目录）
```
- 抓取只用**公开接口直连**（B站 room/card API），绝不碰浏览器自动化；
- `--user` 听课人仅记入 meta.json，不进目录名；
- 产出是本地的，`user/*/` 已被 .gitignore 排除、不入版本库（见 `user/README.md`）。

## 手动流程（仅调试用，正式代听走 listen.py）

散装起停 record/transcribe 仅用于调试单个环节：
```bash
PY="<setup.sh 显示出的 python 路径>"
SESSION="<任意会话目录>"; mkdir -p "$SESSION/chunks" "$SESSION/transcripts"
nohup "$PY" record.py start --session "$SESSION" --segment 5 > "$SESSION/record.log" 2>&1 &
nohup "$PY" transcribe.py --session "$SESSION" --watch > "$SESSION/transcribe.log" 2>&1 &
# …结束…
touch "$SESSION/done.flag"                # 通知转写器收尾
"$PY" record.py stop --session "$SESSION"  # 停止捕获（自动落盘尾块）
```
无需恢复输出设备（本方案从不改动系统输出）。

## 故障速查

| 症状 | 原因与处理 |
|---|---|
| setup 报「屏幕录制」权限未授予 | 系统设置→隐私与安全性→屏幕录制 勾选宿主 App 并重启，见第 2 步 |
| listen 启动即退「内录不可用」 | 权限未授予（如上）；`record.py check` 可单独复查 |
| 全是静音/无文字 | 系统音量 0、直播没在播、标签页静音：调高音量或确认播放（见第 4 步提醒） |
| 转写全为空 | 直播页静音/没点播放/需登录：截屏确认并处理 |
| 盲录平台一直不收尾 | 有背景音/音乐持续出声 → 加大 `--silent-timeout` 或叠加 `--max-minutes` 兜底 |
| 声音忽断 | 网络卡顿属正常，恢复自动续录；持续静音按第 5 步处理 |
| B站误判下播收尾 | 网络波动偶发：`--down` 默认 5 次已较稳，必要时调大 |
| 磁盘占用 | 5s×16k 单声道约 115MB/小时（与分块粒度无关），可接受 |
| 静音段无分块 | 正常设计：有声才落盘，转写只处理实际内容 |

## 已知边界
- 只录系统正在播放的声音（直播内容），不录用户麦克风；
- 受 DRM 保护的媒体（个别平台版权内容）可能被 macOS 静音，属系统限制；
- 录音期间系统通知音会混入，开始前提醒用户静音无关应用；
- 平台登录态在浏览器侧；模型路径可用 `LCC_MODELS_DIR` 覆盖（见 common.py 顶部）；
- 抓主播/主题仅 B站等有公开接口的平台；其余平台目录降级为纯时间，属预期。

## 资源说明
- `scripts/listen.py`：持续听课编排（**推荐入口**：认任意直播URL/近流式出字/自动收尾）；
- `scripts/capture.swift`：系统声音捕获工具（ScreenCaptureKit，编译产物 `scripts/bin/capture`）；
- `scripts/record.py`：捕获启停入口（`check` / `start` / `stop`，调试用）；
- `scripts/transcribe.py`：增量转写器（sherpa-onnx，`--watch` 轮询模式）；**GPU优先/CPU兜底**自动决策
  （mac 通常无 CUDA → 自动用 CPU，行为与之前一致，无需任何改动）；
- `scripts/common.py`：环境探测（python/capture/模型），其他脚本共用；
- `scripts/setup.sh`：一键环境检查/修复（`--fix` 自动建 venv、编译 capture、下载模型）；
- `references/audio-setup.md`：「屏幕录制」权限授权与常见问题（零安装说明）；
- `references/platforms.md`：各平台注意事项与结束判定。
