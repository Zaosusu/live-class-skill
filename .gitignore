# 编译产物（用户自行编译）
scripts/bin/capture

# Python 虚拟环境与缓存
scripts/.venv/
win/scripts/.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# 会话录音残留（本地测试产物，勿入库）
*.log

# 本机 GPU 加速验证环境（本地专用，勿入库）
.gpu/

# 用户听课产出根：user/ 下除规范文档 README 外，其余（产出目录等）都不入版本库。
# 产出结构：user/session/<主播账号>_<直播主题>_<开始时间>/（主播+主题+时间全在目录名）。
# 每场完整产出（音频分块+转写稿+日志）都是本地产出，不进 git。
# user/README.md 已被 git 跟踪，不受此规则影响，始终入库。
user/*/
